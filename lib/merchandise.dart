import 'package:flutter/material.dart';

class merchandise extends StatelessWidget {
  const merchandise({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Merchandise",style: TextStyle(fontSize: 30),),
      ),
    );
  }
}
